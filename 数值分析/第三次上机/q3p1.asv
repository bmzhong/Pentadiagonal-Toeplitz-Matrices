syms x y;
y=1/x;
f=matlabFunction(y);
y1=diff(y);
f1=matlabFunciton(y1);
a=2;  b=1;
max=f(b);
min=f(a);
p0x=(max+min)/2;
%零次最佳一致逼近多项式：
fprintf('f(x)在[1,2]上的零次最佳一致逼近多项式为：%d\n',p0x);
a1=(f(b)-f(a))/(b-a);
x2=solve(y1==a1);
a0=(f(a)+f(x2))/2-a1*(a+x2)/2;
y=